# Name,     Type, SubType, Offset,   Size, Flags
nvs,        data,   nvs,      0x9000,       0x6000,
# camera_face_track (esp32-camera + esp-dl's bundled face-detect model
# weights) pushes the built binary to ~3.63MB -- confirmed via "app
# partition is too small for binary" at build time with the 3M size below.
# 5M leaves comfortable room to grow; still ~5MB free in the board's 16MB
# flash after `model` below (CONFIG_ESPTOOLPY_FLASHSIZE_16MB).
factory,    app,    factory,  0x10000,      5M,
model,      data,   spiffs,   ,             5900K,
